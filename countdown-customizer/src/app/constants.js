export const WHITE_COLOR = "#FFFFFF";
export const DEFAULT_SIZE = 90; 
export const DEFAULT_FONT = "Arial";

export const DEFAULT_COLORS = {
    day: WHITE_COLOR,
    hours: WHITE_COLOR,
    minute: WHITE_COLOR,
    seconds: WHITE_COLOR,
};

export const DEFAULT_SIZES = {
    day: DEFAULT_SIZE,
    hours: DEFAULT_SIZE,
    minute: DEFAULT_SIZE,
    seconds: DEFAULT_SIZE,
};

export const DEFAULT_FONTS = {
    day: DEFAULT_FONT,
    hours: DEFAULT_FONT,
    minute: DEFAULT_FONT,
    seconds: DEFAULT_FONT,
};

export const DEFAULT_DAYS = {
    days: 0,
    hours: 0,
    minutes: 0,
    seconds: 0,
};


export const AVAILABLE_FONTS = [
        "Arial", "Courier New", "Times New Roman", "Verdana", 
        "Georgia", "Tahoma", "Comic Sans MS"
    ];
